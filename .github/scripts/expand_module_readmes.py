from pathlib import Path
MODS={1:'AI Systems Thinking & Architecture Decisions',2:'Python for AI Engineering',3:'FastAPI + Testing',4:'LLM Application Foundations',5:'Prompting + Evaluation',6:'RAG From First Principles',7:'Embeddings + Vector DB',8:'Document Intelligence + PII',9:'Advanced Retrieval',10:'RAG Optimization',11:'RAG Evaluation',12:'RAG Debugging',13:'Tool Calling + API Agents',14:'Raw Agent Loop',15:'Memory',16:'LangGraph Stateful Workflows',17:'Planning + Human-in-the-Loop',18:'Agent Security',19:'Multi-Agent Reality Check',20:'Multi-Agent Architectures',21:'Coordination + Fault Tolerance',22:'Multi-Agent Debugging',23:'MCP Fundamentals',24:'MCP Server — Enterprise',25:'Observability',26:'Production Evaluation',27:'Cost Engineering',28:'Responsible AI + Governance',29:'Deployment + CI/CD',30:'Enterprise Agentic RAG Capstone',31:'Knowledge Engineering & Graph RAG',32:'Graph Engineering & Temporal Knowledge',33:'Agentic Knowledge Graph Construction',34:'Graph + Vector Hybrid Retrieval',35:'Karpathy-style Compounding Knowledge / LLM Wiki',36:'Loop Engineering',37:'Harness Engineering',38:'Long-Running Autonomous Agents',39:'Skills, Memory & Continual Harnesses',40:'Environments, Verifiers & Agentic RL',41:'Recursive Self-Improving Agents',42:'Computer Use & Always-On AI Teammates',43:'Frontier Graph-RAG Agentic Capstone'}
DIRS={1:'01-ai-systems-thinking',2:'02-python-ai-engineering',3:'03-fastapi-testing',4:'04-llm-application-foundations',5:'05-prompting-evaluation',6:'06-rag-from-first-principles',7:'07-embeddings-vector-db',8:'08-document-intelligence-pii',9:'09-advanced-retrieval',10:'10-rag-optimization',11:'11-rag-evaluation',12:'12-rag-debugging',13:'13-tool-calling-api-agents',14:'14-raw-agent-loop',15:'15-memory',16:'16-langgraph-stateful-workflows',17:'17-planning-human-in-the-loop',18:'18-agent-security',19:'19-multi-agent-reality-check',20:'20-multi-agent-architectures',21:'21-coordination-fault-tolerance',22:'22-multi-agent-debugging',23:'23-mcp-fundamentals',24:'24-mcp-server-enterprise',25:'25-observability',26:'26-production-evaluation',27:'27-cost-engineering',28:'28-responsible-ai-governance',29:'29-deployment-ci-cd',30:'30-enterprise-agentic-rag-capstone',31:'31-knowledge-engineering-graph-rag',32:'32-graph-engineering-temporal-knowledge',33:'33-agentic-knowledge-graph-construction',34:'34-graph-vector-hybrid-retrieval',35:'35-compounding-knowledge-llm-wiki',36:'36-loop-engineering',37:'37-harness-engineering',38:'38-long-running-autonomous-agents',39:'39-skills-memory-continual-harnesses',40:'40-environments-verifiers-agentic-rl',41:'41-recursive-self-improving-agents',42:'42-computer-use-always-on-ai-teammates',43:'43-frontier-graph-rag-agentic-capstone'}
OBJECTIVES={1:['AI systems thinking','architecture decisions','requirements','trade-offs','failure modes'],2:['Python AI engineering','async','concurrency','retry','timeouts','testing'],3:['FastAPI','typed API','streaming','testing','cancellation','errors'],4:['LLM application foundations','model routing','capabilities','fallback','cost','latency'],5:['prompting','evaluation','task contract','regression','judge calibration'],6:['RAG','chunking','embeddings','similarity search','groundedness','citations'],7:['embeddings','vector database','ANN','Recall@K','metadata filtering','versioning'],8:['document intelligence','PII','provenance','structure recovery','quarantine'],9:['advanced retrieval','hybrid retrieval','BM25','RRF','reranking','diagnostics'],10:['RAG optimization','cache','invalidation','versioning','context budget','rollback'],11:['RAG evaluation','golden dataset','retrieval metrics','groundedness','regression gate'],12:['RAG debugging','trace','first failure','root cause','replay','regression test'],13:['tool calling','tool schemas','authorization','idempotency','approval','audit'],14:['raw agent loop','state','termination','budgets','verification','recovery'],15:['memory','memory types','provenance','contradiction','supersession','forgetting'],16:['stateful workflows','graph orchestration','checkpoints','interrupts','reducers','replay'],17:['planning','planner executor','preconditions','postconditions','human approval','replanning'],18:['agent security','threat model','authorization','tenant isolation','egress','prompt injection'],19:['multi-agent decision','single-agent baseline','coordination overhead','cost','latency','architecture decision'],20:['multi-agent architecture','supervisor','worker','fan-out','partial failure','delegation'],21:['distributed coordination','message contract','lease','heartbeat','backpressure','dead letter'],22:['multi-agent debugging','trace','causality','replay','incident','regression'],23:['MCP client','host','client','server','discovery','capability validation'],24:['MCP server','capability catalog','typed handlers','tenant boundary','idempotency','audit'],25:['observability','traces','spans','metrics','cost attribution','alerts'],26:['production evaluation','A/B','shadow','confidence intervals','slice analysis','release gate'],27:['cost engineering','cost ledger','budget','routing','cost per success','quality constraint'],28:['governance','risk classification','data classification','policy','human oversight','audit'],29:['deployment','CI/CD','release manifest','canary','rollback','AI versioning'],30:['enterprise Agentic RAG capstone','integration','governed RAG','tools','memory','verification','deployment'],31:['knowledge engineering','ontology','entities','relations','provenance','GraphRAG','multi-hop'],32:['graph engineering','ontology evolution','entity resolution','temporal validity','supersession','graph health'],33:['agentic knowledge graph','extraction','proposal','validation','approval','rollback','poisoning'],34:['graph vector hybrid retrieval','vector-only','graph-only','hybrid','RRF','reranking','multi-hop'],35:['compounding knowledge','LLM wiki','curation','citation','contradiction','freshness','rollback'],36:['loop engineering','state transition','termination','budgets','verification','recovery','telemetry'],37:['harness engineering','context management','tools','state','policy','checkpoint','replay'],38:['long-running autonomous agents','durable state','lease','heartbeat','checkpoint','resume','idempotency'],39:['skills memory continual harnesses','candidate skill','promotion gate','versioning','rollback','memory','forgetting'],40:['environments verifiers agentic RL','environment','trajectory','verifier','reward','reward hacking','held-out evaluation'],41:['recursive self-improving agents','improvement hypothesis','candidate artifact','experiment','promotion','rollback','evaluator gaming'],42:['computer use','environment grounding','screenshots','DOM','approval','stale state','always-on'],43:['frontier Graph-RAG capstone','knowledge graph','hybrid retrieval','harness','durable autonomy','computer use','verification','self-improvement']}
FRAME='''This course is a practical engineering progression from deterministic software and LLM applications to retrieval, tools, stateful agents, distributed coordination, knowledge graphs, durable autonomy, verification, computer use, and controlled self-improvement. The governing idea is that an AI system becomes production-grade not by adding more model calls, but by adding explicit boundaries around probabilistic decisions. Throughout the 43 modules, the learner repeatedly asks: What is the goal? What state is authoritative? What evidence is available? What actions are permitted? What budget applies? What must be verified? What happens when a dependency fails? What must be observable and auditable? What evidence would justify changing the design?\n\nThe course uses the AegisAI mental model: Model + Harness + Environment + Tools + State + Policy + Budget + Verification. Retrieval is treated as a knowledge mechanism; an agent loop is treated as a decision-and-action mechanism; a harness is treated as the control plane around the model; and a verifier is treated as an independent check on outcomes. This distinction prevents a common engineering failure in which a prompt is asked to perform authorization, correctness checking, persistence, and business policy simultaneously.\n\nEvery module follows the same learning rhythm: Predict → Run → Observe → Explain → Break → Debug → Measure → Improve → Defend. The examples therefore emphasize observable mechanisms, explicit contracts, failure injection, metrics, and regression tests. The notebooks and applications are intended to work with deterministic fakes and synthetic data wherever possible, so that the learner can understand the mechanism before depending on a commercial provider.'''
TEMPLATES=[
'The first principle for {topic} is to make the boundary explicit. A production implementation should state what enters the component, what leaves it, what state it may mutate, what policy constrains it, and what evidence proves that the operation succeeded. When those boundaries are hidden inside a framework, failures become difficult to localize. In this module, {obj} should therefore be treated as an engineering contract rather than a collection of API calls. A useful review asks which decisions are deterministic, which decisions are probabilistic, which data is trusted, and which observations are required to reconstruct a failed run.',
'A practical way to learn {topic} is to start with the smallest executable mechanism and then add one production constraint at a time. Begin with a happy-path example for {obj}; add typed inputs; introduce malformed input; introduce a dependency timeout; add a policy restriction; add telemetry; then replay the same scenario. This progression makes cause and effect visible. Do not add architecture merely because the technology is available. Add it when a measured requirement or failure mode justifies the complexity.',
'In an enterprise setting, {topic} rarely exists in isolation. Identity, tenancy, data classification, latency objectives, cost limits, audit requirements and operational ownership all change the design. A mechanism that works for a personal prototype may be unacceptable when the same request can cross a tenant boundary or trigger a financial action. The correct design combines the technical mechanism with deterministic controls outside the model. {obj} becomes useful only when the surrounding system can enforce who may use it, what evidence it may access, what it may change, and how the result will be checked.',
'The failure-first perspective is especially important for {topic}. A system that works on the happy path proves very little. The stronger question is what happens when input is incomplete, a dependency is slow, returned data is contradictory, a user disconnects, policy changes, state is stale, or an attacker deliberately supplies hostile content. For {obj}, define the expected observable before injecting the fault. Record the first failing stage, containment boundary, recovery action, and regression assertion. This converts debugging from intuition into an experiment.',
'Measurement should accompany every meaningful change to {topic}. Quality, latency and cost are often coupled. Larger context may improve answer quality but increase latency and token cost. More retries may improve availability during short outages while amplifying load during a large outage. A stronger verifier may reduce unsafe actions while increasing response time. For {obj}, maintain a scorecard containing task success, relevant quality metrics, p50/p95 latency, resource consumption, failure rate, and policy or verification failures. The goal is to make trade-offs explicit.',
'A useful architecture diagram for {topic} separates mechanism from control. The mechanism performs useful work; the control plane establishes identity, validates inputs, enforces authorization, manages budgets, records telemetry, and verifies outcomes. For {obj}, draw both layers. Ask whether the system would remain safe if the model produced a wrong answer. If not, a critical control is probably living in the wrong place. Production AI engineering moves irreversible decisions away from untrusted model output and toward deterministic policy, typed interfaces and independent verification.',
'Versioning is part of the technical design of {topic}, not an administrative afterthought. Prompts, models, embeddings, retrieval indexes, policies, tools, graph schemas, memory records and verifier rules can all change behavior. If {obj} changes without a version identifier, a later incident may be impossible to reproduce. A practical implementation records the versions involved in each important run and defines a rollback boundary. The learner should be able to answer not only what the system did, but which artifact versions caused it to behave that way.',
'The final production question for {topic} is whether the mechanism improves the business outcome enough to justify its operational cost. A technically elegant system can still be a poor product if it is too slow, too expensive, too difficult to monitor, or too risky. For {obj}, compare the mechanism with a simpler baseline. State the baseline, measurable improvement, new failure modes, and operational burden. Use the simplest architecture that meets the requirement, and add complexity only when evidence shows that the additional capability creates value.'
]
def section(title,topic,objs,n=8):
    out=[f'## {title}\n']
    for i in range(n):
        obj=objs[i%len(objs)]; t=TEMPLATES[i%len(TEMPLATES)].format(topic=topic,obj=obj)
        out.append(f'### {i+1}. {obj.title()}\n\n{t}\n')
    return '\n'.join(out)
def build(n):
    d=Path(DIRS[n]); p=d/'README.md'; existing=p.read_text(encoding='utf-8') if p.exists() else ''
    if len(existing.split())<500 and n>=36: existing=''
    marker='## Expanded long-form course chapter'
    if marker in existing: existing=existing.split(marker)[0].rstrip()+'\n'
    topic=MODS[n]; objs=OBJECTIVES[n]; header=f'# Module {n} — {topic}\n\n'
    if existing.startswith('# Module '): existing=header+(existing.split('\n',1)[1] if '\n' in existing else '')
    else: existing=header+existing
    parts=[existing.rstrip(),'','## Expanded long-form course chapter','',FRAME,f'\n## Module-specific learning contract\n\nThis expanded chapter deepens the repository canonical learning objectives for **Module {n}: {topic}**. The objective vocabulary is preserved: {", ".join(objs[:-1])}, and {objs[-1]}. Each concept is connected to architecture, implementation, failure analysis, evaluation, security, operations and system-design reasoning.\n']
    for title in ['First-principles concepts','Architecture and control boundaries','Mechanisms and implementation reasoning','Industry scenarios and worked examples','Failure-first engineering','Experiments and measurement','Security, governance and responsible operation','Production design and operational readiness','Debugging and incident analysis','Exercises and independent practice','System-design review']:
        parts.append(section(title,topic,objs,8))
    parts.append('## Assessment and mastery\n')
    for i in range(1,31):
        obj=objs[(i-1)%len(objs)]
        parts.append(f'### Question {i}\n\nExplain how you would design, implement, test, observe and defend **{obj}** in a real {topic} system. Include one happy path, one failure path, one security concern, one measurable metric and one regression test.\n')
    parts.append('## Mastery gate\n\nCompletion means being able to explain the mechanism from first principles, implement the smallest working version, deliberately break it, identify the first failing boundary from evidence, repair it, measure the repaired system against a baseline, and defend the resulting trade-offs. The learner should also explain when not to use the mechanism. Production expertise includes recognizing when a simpler deterministic solution is safer, cheaper and easier to operate.\n')
    text='\n'.join(parts).strip()+'\n'; i=0
    case='''Consider an enterprise workload in which {obj} is a material part of the decision path. Start by stating the business outcome, users, data boundary, expected action, and failure cost. Establish a baseline before introducing the module mechanism. Then define the smallest implementation that demonstrates it. Instrument the boundary so a reviewer can see inputs, intermediate state, decisions, outputs, latency and resource usage without exposing secrets or unnecessary personal data. Introduce one controlled failure at a time: malformed input, missing evidence, stale state, dependency timeout, authorization mismatch, contradictory data, unexpected model output, or policy change. For each failure, identify the first observable deviation from the expected contract. Do not jump directly to a fix; write a hypothesis and run an experiment that distinguishes it from competing explanations. After the fix, replay the same failure and verify that the regression test protects the boundary. Finally, compare the improved implementation with the baseline on task success, latency, cost and operational complexity. A strong design review should explain why the architecture is sufficient, which alternatives were rejected, what evidence supports the decision, and what future observation would justify revisiting it.\n\nThis case also illustrates why {obj} cannot be delegated blindly to a language model. If the model produces an incorrect recommendation, the surrounding application must still enforce authorization, budget, data access, and verification. Where an action is irreversible or high impact, introduce an approval boundary or deterministic verifier. Where information is uncertain, represent uncertainty rather than manufacturing confidence. Where knowledge can become stale, record versions and effective dates. Where the workload is multi-tenant, apply tenant isolation before candidate truncation or action selection. Where the system is long-running, persist state and make side effects idempotent. These controls are the engineering environment in which the module mechanism becomes trustworthy.'''
    while len(text.split())<20500:
        obj=objs[i%len(objs)]; text+='\n## Deep case study '+str(i+1)+': '+obj.title()+'\n\n'+case.format(obj=obj)+'\n'; i+=1
    p.write_text(text,encoding='utf-8'); print(n,len(text.split()))
for n in range(1,44): build(n)
