# Module 34 Mastery Gate

Given a fixed benchmark, build vector-only, graph-only, union/RRF and reranked hybrid retrieval. Measure quality, latency, traversal work, tokens and cost; inject graph, vector, poisoning and authorization-order failures; and defend a ship/reject decision.

Pass evidence: runnable notebook, benchmark report, failure diagnosis, security test, optimization experiment and ADR.

## L7 question
Hybrid improves Recall@20 by 7% but doubles p95 and triples cost. What additional evidence is needed before adoption, and what workload might still justify it?